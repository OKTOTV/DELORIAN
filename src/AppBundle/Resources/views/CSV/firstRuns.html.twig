{% extends "BprsStyleBundle::layout.html.twig" %}
{% block body_main %}
    <div class="page-header">
    <h1>[{{series.abbrevation}}] {{series.title}}</h1>
    <div>
    {{ form_start(form) }}
    {{ form_errors(form) }}
    {{ form_row(form.from) }}
    {{ form_row(form.to) }}
    {{ form_end(form) }}
{% endblock %}